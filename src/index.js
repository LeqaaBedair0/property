import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './property-card-project/App';
import './index.css';  // We'll style everything later

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
